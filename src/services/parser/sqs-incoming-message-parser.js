// import { logInfo } from '../../middleware/logging';

export const parse = async () => {
  // logInfo('Trying to parse ehr-out-service-incoming event');
  //
  // const {
  //   id: conversationId,
  //   attributes: { interactionId, ehrRequestId, nhsNumber, odsCode }
  // } = await JSON.parse(message);
  //
  // return { conversationId, interactionId, ehrRequestId, nhsNumber, odsCode };
};
